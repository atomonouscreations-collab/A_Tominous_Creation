# software/plugins

Plug-ins that wrap already-existing external methods — planar slicers, non-planar slicers, CAM software, laser tools — so they can be invoked from inside the ecosystem host rather than run as separate programs. Each plug-in implements the interface defined by `software/slicer-integration/`. Empty for now — structure only.
